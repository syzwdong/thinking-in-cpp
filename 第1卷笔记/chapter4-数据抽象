C与C++的struct
C中struct相应的函数在struct外部，这会带来一些不便甚至细微的bug
C++中struct相应的函数可以放在内部，作为成员函数

作用域解析运算符::
this, 相应struct的地址

C++中，其他类型的指针可以赋值给void *，反之不行，需要强制转换

封装 将数据连同函数捆绑在一起的能力可以用于创建新的数据类型，实现细节的隐藏

C和C++都允许重声明函数，但两者都不允许重声明struct
预处理
#ifndef
#define
#endif

不要在头文件中放置using